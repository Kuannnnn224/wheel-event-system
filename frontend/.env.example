# Leave empty to use the Vite /api proxy.
# Set an absolute URL only when the frontend is served without a /api reverse proxy.
VITE_API_BASE_URL=

# Optional dev proxy target for /api.
VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:3001
